Delete a storage place.
---
security:
  - basicAuth: []
parameters:
  - name: x
    in: query
    required: true
    description: "Storage place name that should be deleted."
    schema:
      type: string
      example: "HAL-12;2;3;1"
responses:
  default:
    description: "Successful operation."
